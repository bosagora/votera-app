import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    backspace: {
        alignItems: 'center',
        flex: 1,
        justifyContent: 'center',
    },
    cell: {
        alignItems: 'center',
        height: 48,
        justifyContent: 'center',
        width: 112,
    },
    container: {},
    number: {
        fontSize: 25,
        textAlign: 'center',
    },
    row: {
        flexDirection: 'row',
        marginTop: 15,
    },
});
